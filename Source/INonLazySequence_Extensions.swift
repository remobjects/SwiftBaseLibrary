//public typealias SequenceType<T> = INonLazySequence<T>

public protocol INonLazySequence<T> {
	// Todo: redefine each platform's core ISequence<T> methods.
}

public extension INonLazySequence {
	// Todo: reimplement all the ISequence<T> extensions, but non-lazy.
}